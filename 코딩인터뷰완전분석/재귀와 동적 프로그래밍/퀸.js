`문제 8x8 체스판에 8개의 퀸이 서로 공격할 수 없도록 하는 경우의 수는?`;

const solution = () => {
  const rowSet = new Set();
  const colSet = new Set();
  const diaSet = new Set();

  const checkDia = (r, c, rowSet, colSet) => {};

  const dfs = (newRow) => {
    for (let c = 0; c < 8; c++) {}
  };

  dfs();
};
